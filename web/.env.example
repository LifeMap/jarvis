# Browser-visible API path. Keep API tokens out of VITE_* variables.
VITE_AGENT_API_URL=/api/agent/message

# Local Vite proxy settings (server-side only).
AGENT_API_ORIGIN=http://127.0.0.1:8787
JARVIS_API_TOKEN=replace-me
