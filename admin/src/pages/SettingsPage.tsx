import{FormEvent,useCallback,useEffect,useState}from"react";import{settings}from"../services/admin";import{ErrorBanner,Loading}from"../components/AsyncState";import{useLoad}from"../hooks/useLoad";
export function SettingsPage(){const load=useLoad(useCallback(settings.get,[]));const[language,setLanguage]=useState(""),[timezone,setTimezone]=useState("");useEffect(()=>{if(load.data){setLanguage(load.data.language);setTimezone(load.data.timezone)}},[load.data]);if(load.loading)return <Loading/>;if(load.error)return <ErrorBanner message={load.error}/>;const save=async(e:FormEvent)=>{e.preventDefault();load.setData(await settings.update({language,timezone}))};return <section className="card settings"><h2>Agent 설정</h2><form onSubmit={save}><label>기본 언어<input value={language} onChange={e=>setLanguage(e.target.value)}/></label><label>시간대<input value={timezone} onChange={e=>setTimezone(e.target.value)}/></label><label>LLM 제공자<input value={load.data!.llmProvider} disabled/></label><label>모델<input value={load.data!.llmModel} disabled/></label><label>보안 값 관리 위치<input value={load.data!.secretsManagedBy} disabled/></label><button>저장</button></form><p>제공자, 모델, API 키와 OAuth 보안 값은 Cloudflare 설정에서 관리되며 이 화면에는 노출되지 않습니다.</p></section>}
