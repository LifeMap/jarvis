import{describe,expect,it}from"vitest";import{detectCapabilityGap}from"../src/capability-gap/capability-gap-detector";
describe("Capability Gap detector",()=>{
  it("classifies explicit unsupported integration mutations",()=>{expect(detectCapabilityGap("Notion 데이터베이스에 회의 내용을 등록해줘",[],[])).toMatchObject({service:"notion",requestedCapability:"Notion 페이지/데이터베이스 작성"});expect(detectCapabilityGap("이 PDF에 전자서명해줘",[],[])).toMatchObject({service:"pdf"})});
  it("does not classify discussion, supported failures, or registered MCP capabilities",()=>{expect(detectCapabilityGap("Notion 연동은 좋은 것 같아",[],[])).toBeNull();expect(detectCapabilityGap("Gmail OAuth가 만료됐어. 다시 연결해줘",[],[])).toBeNull();expect(detectCapabilityGap("Calendar Provider timeout을 확인해줘",[],[])).toBeNull();expect(detectCapabilityGap("Slack으로 메시지 보내줘",[],[{id:"slack",name:"Slack MCP",endpoint:"https://example.test/mcp",transport:"streamable-http",enabled:false,authType:"oauth",service:"search",providerId:"slack-mcp",capabilityMapping:{},createdAt:"",updatedAt:""}])).toBeNull()});
  it("recognizes capabilities from generically named discovered MCP tools",()=>{expect(detectCapabilityGap("Notion에 페이지를 작성해줘",[],[{id:"work",name:"Work tools",endpoint:"https://example.test/mcp",transport:"streamable-http",enabled:true,authType:"none",service:"search",providerId:"work-mcp",capabilityMapping:{},createdAt:"",updatedAt:""}],["notion_create_page"])).toBeNull()});
});
